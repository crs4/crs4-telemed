<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="it.crs4.most.demo.ecoapp">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/app_icon"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".EcoConfigActivity"
            android:label="@string/remote_app">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".SettingsActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".EcoConfigActivity"/>
        </activity>
        <activity
            android:name=".EcoTeleconsultationActivity"
            android:label="@string/title_activity_teleconsultation"
            android:screenOrientation="portrait"/>

        <activity
            android:name=".AREcoTeleconsultationActivity"
            android:label="@string/title_activity_teleconsultation"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar.FullScreen"
            />
        <receiver android:name=".AREcoTeleconsultationActivity$RemoteControlReceiver">
            <intent-filter android:priority="999">
                <action android:name="android.intent.action.MEDIA_BUTTON" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
